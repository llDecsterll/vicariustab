---
description: Индекс Vicariustab v2.0.16 — карта модулей, тесты, документация
globs: product/**
alwaysApply: false
---

# Vicariustab — индекс (git repo)

**v2.0.16** · https://github.com/llDecsterll/vicariustab

## Полный каталог

→ **`docs/FILES_INDEX.md`** — 89 src (39 components + 41 utils/config/legal), server 18, tests 11  
→ Workspace: **`../FILES_INDEX.md`** · Keyserver (локально): **`../keyserver/LOCAL_INDEX.md`**

## Быстрый старт

```bash
npm install && npm run dev
npm run lint && npm run test:unit && npm run test:audit
```

## Критичные модули

| Задача | Файлы |
|--------|-------|
| Склад + списание | `WarehouseView.tsx`, `markPendingWriteOff.ts`, `cancelPendingWriteOff.ts`, `warehousePendingMerge.ts` |
| Excel | `warehouseExcel.ts`, `xlsx` |
| Группы | `warehouseRouting.ts`, `WAREHOUSE_TYPE_TAB` |
| Лицензия | `license.ts`, `licenseCore.ts`, `apiAuth.ts` |
| Backup | `backupLicensePolicy.ts`, `SettingsView.tsx` |
| Update | `updateCheck.ts`, `updateEngine.ts`, `githubUpdateCheck.ts` |

## Gated (trial без ключа)

Excel · Backup JSON/.enc · Update apply (`canUseSystemUpdate`)

## Keyserver (вне git)

Приватный PEM: `../keyserver/data/license_ed25519.pem` — **не коммитить**  
Публичный ключ: `server/licensePublicKey.ts`

## Документация

`AGENTS.md` · `docs/FILES_INDEX.md` · `docs/RELEASE_AUDIT_v2.0.16.md`

## Cursor rules (workspace)

`../.cursor/rules/` — product-vicariustab, equipment-lifecycle, server-backend, keyserver-local
